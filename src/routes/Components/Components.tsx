import { fs_regular, fw_regular, Text } from "@bennyui/core";
import React from "react";

const Components = () => {
	return (
		<Text color="white" fontSize={fs_regular} fontWeight={fw_regular}>
			This is components
		</Text>
	);
};

export default Components;
